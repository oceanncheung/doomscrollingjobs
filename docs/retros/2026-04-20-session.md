# 2026-04-20 — claude-harness-overhaul

## Goal
Audit current Claude Code setup against a Reddit article on harness improvements; apply the actionable wins without over-engineering.

## What landed
- `a7dd5b4` docs: introduce CLAUDE.md as canonical AI entrypoint, dedupe linkage
- `c1a6c67` fix(profile): re-sync LinkedIn input after router.refresh()
- `5c8872b` fix(profile): top-level safety net so unhandled errors surface as banner
- `0abe0a4` docs: add docs/retros/ for session continuity
- (this commit) chore: add Claude Code hooks (Stop + SessionStart) + refine .gitignore

Cleanups (no commits — untracked or gitignored): deleted dead `profile-testing-reset-button.tsx` + `job-workflow-controls.tsx`; removed 17+ `.next/...d 2.*` Finder duplicates and `.next 2/` directory; nuked 4 stale Claude worktrees + branches + broken `main 2`/`origin/main 2` refs.

## What worked
- **Auditing before building.** Most "missing" items from the article were already present (typed memory, doc system, custom eval harness) — only real gaps got new code.
- **User pushback on "salvage now".** Forced actual verification of the abandoned worktree code instead of trusting assumptions; led to confirming both bugs were real and not duplicated.
- **Soft scope thresholds.** Chose pause-and-explain at 5 files / 100 lines over hard 50-line caps (would've gamed the iOS multi-file fixes).
- **Pipe-testing hook commands** before writing settings.json — caught escape-quoting issues early.

## What didn't / friction
- Initial overconfidence on worktree salvage — labeled both fixes as "valuable, apply now" without checking whether main already had them.
- `preview_fill` synthetic input changes don't always trigger React 19's form-data serialization → Bug 1 verification was inconclusive in browser; relied on structural evidence + your own 2026-04-18 repro note instead.
- Generate Profile button disabled (already used) → couldn't directly repro Bug 2 without resetting profile state.
- Conflated dead "untracked WIP" with active WIP at first (the testing-reset button) — should've grepped for imports up front.

## Decisions
- **CLAUDE.md and AGENTS.md stay separate** — project-wide vs UI-specific governance, no sync risk if ownership boundary holds.
- **Hooks: skip per-Edit typecheck** (too noisy) → fold into Stop hook when source files are dirty.
- **Refine .gitignore** to track `.claude/settings.json` + `launch.json` but keep `settings.local.json` + `worktrees/` ignored. Project-wide harness config now shareable.

## Open / next session
- **Push 5 commits to `origin/main`** — will trigger Cloud Run deploy; need to start the deploy monitor afterward (per the post-push memory rule).
- **context7 MCP install** — last item from original audit list, low-priority.
- **`next-env.d.ts` modification** — Next 15 dev-build auto-rewrote the import path. Leave (it'll regenerate anyway) or revert? Decide on next dev cycle.
- **Restart Claude Code** for the new hooks to take effect — the settings watcher only watches `.claude/` dirs that existed at session start. The hooks are written correctly per `jq -e` validation; they just need a session restart (or `/hooks` open) to register.
